Start the IDS prototype dev server.

## What to Do

1. Run `npm run dev` to start the Vite dev server.
2. Tell the designer to open **http://localhost:5174** in their browser.
3. If the server is already running, let them know.
4. If there are errors (missing packages, syntax issues), fix them automatically and try again.
